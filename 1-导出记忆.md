# 第一步 · 在老窗口里导出记忆

**这一份贴进你现在那扇窗**——就是已经在跟你项目打交道、脑子里有上下文的那一扇。

它只做一件事：**把这个项目的记忆写成一份导出件。**

> ★**这一步不改你任何现有文件。** 只新建一个 `记忆导出.md`。
> 你原来的 `CLAUDE.md` / `AGENTS.md` / 章程 / 笔记，一个字都不动。

---

## 贴这一句

```
读 1-导出记忆.md，按它的模板把本项目的记忆导出成 记忆导出.md。
只新建这一个文件，不许改动任何现有文件。
导出件不要超过 6000 字，超了就按它说的取舍。
```

---

## 硬约束（四条，一条都不能破）

1. **只新建 `记忆导出.md`，不改任何现有文件。** 包括不许"顺手整理格式"。
2. **硬规矩必须原文照抄**，逐条标出处文件名。不许改写、不许概括、不许"优化措辞"。
3. **总量不超过 6000 字。** 装不下时按下面的取舍规则砍，并在第七节写明砍了什么。
4. **不确定的不要编。** 答不上来就写「不清楚」或「要问人」，这比猜一个准确得多。

> 为什么卡 6000 字：记忆文件分两类，体量差着一到两个数量级。
> **规矩类**（章程、格式约定）改的时候回原处修改，通常只有几千字；
> **流水类**（收工播报、卡点、技术决定）按时间往下追加，
> 可以长到规矩类的几十倍。
>
> 整套搬进新窗口会当场撑爆，而且爆的是沟通那一侧，
> 它会开始丢你最早说的话，还不给任何提示。
> **导出不是备份，是交接。** 交接要的是「下一个人接手需要知道什么」，
> 不是「这个项目发生过什么」。

---

## 导出模板（照这个七节写）

```markdown
# 记忆导出 · <项目名> · <日期>

## 一、这个项目是什么
<三句话以内。做什么的、给谁用的、现在到哪一步了。>

## 二、硬规矩（★原文照抄，不许改写）
逐条列，每条标出处：
- [出处文件名] <原文整条>
- [出处文件名] <原文整条>
…
★包括：不许碰什么、怎么算做完、有没有写明理由。
★这一节优先级最高，字数不够先砍别的节。

## 三、怎么协作的
- 现在同时有几扇窗/几个 AI 在干活：
- 谁派活、谁验收：
- 交接靠什么（git / 某个目录 / 口头）：
- 有没有台账（工单、清单、看板），叫什么：

## 四、现在手上在办的事
<最多 10 条，一条一行。做到哪一步了。没有就写"无"。>

## 五、已知的坑（挑现在还成立的，最多 15 条）
<一条一行。每条后面尽量带一句"它防的是哪次事故"。>
★说不出防的是什么的，直接不要导出——那多半是抄来的。

## 六、文件清单与优先级
| 文件 | 规矩类 / 流水类 | 大致体量 | 备注 |
|---|---|---|---|
★「谁压过谁」如果原来就写明了（比如"两文件冲突时以 X 为准"），
  在这里原样写出来。没写明就写「没写明」。

## 七、这次导出没带什么
<列出被砍掉的内容和原因，让下一扇窗知道哪里还能回来捞。>
```

---

## 字数不够时的取舍顺序

从上往下砍，**第二节永远最后砍**：

1. 先砍**第五节**的坑，只留最疼的那几条
2. 再砍**第四节**在办的事，只留还没做完的
3. 再把**第三节**压成几行
4. **第二节硬规矩一条都不许砍**——砍了它，接手就失去意义了

★砍掉的东西要在**第七节**记一笔。不许默默丢。

---

## 规矩类 / 流水类怎么分

**一份文件如果是「按时间往下追加」的**（交接播报、变更日志、决定记录），
它就是**流水类**，不管名字叫什么——**导出时只写它存在、多大，不要搬内容**。

**改的时候会回去修改原处的**（章程、规矩、格式约定），是**规矩类**——
第二节要把它们的硬条款原文搬过来。

---

## 导完之后

你会得到一个 `记忆导出.md`。**下一步换一扇新窗口**，
按 [`2-导入与接手.md`](2-导入与接手.md) 走。

★**老窗口到此为止。** 不用在老窗口里装任何东西、也不用在那边改任何规矩。
