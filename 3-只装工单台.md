# 另一条路 · 只装工单台（纯净版）

**给谁用**：你那套工作方式已经跑得好好的，不想被任何"方法论"打扰，
但缺一个**台账**——记不住现在有哪几件事在办、谁交了没交、哪张单卡住了。

这条路只给你一个**工具**。

---

## 这条路不做什么

- ❌ **不要求你读任何方法文档**——[`1-导出记忆.md`](1-导出记忆.md) 和
  [`2-导入与接手.md`](2-导入与接手.md) 那两步，这条路全部跳过
- ❌ **不导出、不导入、不做兼容检查**
- ❌ **不改你任何规矩**，不问你要不要合并工作架构
- ❌ **不碰你项目里任何现有文件**——工单台的数据默认落在
  `~/.ticket-desk/`，和你的项目目录完全分开

装完你的项目**一个文件都没变**。不合用，删掉那个目录就等于没装过。

---

## ★ 但有一件事必须先说清楚

**这个工具是有主张的，不是中立的记事本。** 说"纯净"不代表它没有立场——
它在两个地方会拦你，装之前你得知道，不然用两天就会觉得"这破玩意儿真啰嗦"：

### 建单时，这四样必填

| 必填项 | 它防的是 |
|---|---|
| **实机消费者** | 写清这东西做完之后由谁加载/谁调用。写不出来的活，多半还没想清楚 |
| **交付项** | 逐行写出必须产出的文件路径。交板时会逐条核存在性 |
| **用户可感知 / 内部工具** 二选一明写 | 漏标不默认为内部——「忘了标」和「确实是内部单」在台账里长得一样，会一直藏到交板那一刻才炸 |
| **任务档** | 这张单的难度档位 |

### 交板时，这两样必给

- **内部单**：验证命令 + **原样输出**（不是"我跑过了，通过"）
- **用户可感知单**：真实环境的证据图

> 这几道闸就是这个工具的全部主张，来自「自述不算数」这一条。
> **你接受不了，这条路就不适合你**——与其装了之后天天想绕过去，
> 不如不装。被绕过的流程比没有流程更坏，因为台面上还写着它在跑。

除此之外它不管你别的事：怎么写代码、怎么分支、怎么开窗、用什么模型，
它一概不问、也不拦。

---

## 最省事：把这一段发给你的 AI

老手也不该为了装个台账工具去啃配置。打开任意**能执行命令**的 AI
（桌面版 / 命令行 / IDE 插件），把下面**整段**发给它——**包括那句"先问我"**：

```
装一个工单台，按这五步做，做完报结果：

1. git clone https://github.com/comkocs/md-first.git
   只用里面的 ticket-desk/ 目录，仓里其余文件不用看也不用管。
2. cd md-first/ticket-desk && python -m pytest tests -q
   应当 521 passed。不是全绿就停下来告诉我，别往下做。
3. ★先问我这三件事，等我回答，不要自己猜：
   · 我有哪几个岗位？（比如：前端、后端、我自己拍板）
   · 我怎么称呼"拍板的那个人"？（比如：我 / 老板 / 设计者）
   · 我的生产环境叫什么？（比如：生产环境 / 线上 / App）
4. 按我的回答写 ticket_desk/config.json。规则：
   · 「位名」= 我说的岗位，再加三个固定角色位：复检、平台、内容
     （名字随我改，但这三个必须在名册里）
   · 「总编排位」「复检位」「平台位」「内容位」四个值必须各是「位名」里的一个
   · 「拍板人」不能和任何一个位重名
   · 不上服就写 "部署目标": []
   · 其余键不用写，留默认
5. python ticket_desk/ticket.py staff list
   跑通说明配置自洽。把最终的 config.json 原样贴给我看一眼。

全程不要改我项目里的任何文件，工单台的数据默认落在我的用户目录，与项目分开。
```

**第 3 步是关键**——它会停下来问你岗位，你用大白话答就行，不用碰 JSON。
配置不自洽它当场报错，不会装到一半留个半成品。

> 不想让 AI 代劳、想自己敲的话，往下看「自己装」。

---

## 自己装（30 秒）

```bash
git clone https://github.com/comkocs/md-first.git
cd md-first/ticket-desk
python -m pytest tests -q                    # 521 条，应当全绿
python ticket_desk/ticket.py env             # 一行说清当前是本机模式还是远程模式
python ticket_desk/ticket.py staff list      # 名册（第一次跑会顺手建库）
```

需要 Python 3.10+。**不需要**服务器、数据库、联网——单机模式什么都不配就能用。
只有图片相关功能需要 Pillow（`pip install pillow`）。

> 工单台住在 md-first 那个仓里，但你**只用 `ticket-desk/` 这个目录**，
> 仓里那四份方法文档不用看、也不用管。

---

## ★ 第一件事：把名字改成你自己的叫法

配置里那套位名、模型名**全是示例**，不是给你用的。
更要紧的是：**用你原来的词，别学它的词。**

你叫「总工程师 / 研发工位 / 复核」，就照原样填进去，
不要为了迁就工具改成「策划窗 / 工位窗 / 验收」——
换词的代价是你自己以后看台面时得在脑子里翻译一遍。

```bash
cp ticket_desk/config.example.json ticket_desk/config.json
```

```jsonc
{
  "产品名": "<你的项目名>",
  "位名": ["<你的岗位1>", "<你的岗位2>",
          "<复核那位>", "<维护工单台那位>", "<管文案真源那位>", "<统筹那位>"],
  "总编排位": "<统筹那位，要和上面名册里的写法一模一样>",
  "复检位":   "<复核那位>",
  "平台位":   "<维护工单台那位>",
  "内容位":   "<管文案真源那位；没有这个角色就填一个现有位>",
  "只分发不派单位": [],
  "拍板人": "<你自己怎么称呼，比如 我 / 老板 / 设计者>",
  "主场景": "<你的生产环境叫什么>",
  "部署目标": []
}
```

**四条硬规矩**（配置不自洽会当场报错，不会带着病往下跑）：

1. **四个特殊位**（总编排位 / 复检位 / 平台位 / 内容位）**的值必须各是「位名」里的一个**，
   一个字都不能差。
2. **「拍板人」不能和任何一个位重名**——拍板的是人，不是一个工位。
3. **「位名」里不能重名。**
4. ★**改了位名，就必须同时管「部署目标」。** 它默认引用的是示例名册里的位名，
   你把位名换成自己的之后，那个引用就悬空了，**任何命令都启动不了**，
   而报错指向「部署目标的归位不在名册里」，跟你刚才改的那行看着毫无关系。
   不上服的话直接写 `"部署目标": []` 最省事；要上服就写成
   `[{"名字":"production","值面键":"deploy_head","归位":"<你名册里的某一位>"}]`。

> 这几道报错是故意的：最坏的失败形态是「单建出去了，通知落进一个不存在的对话线」，
> 那种错没有任何人会发现。宁可开头多报两次错，也不要中间静默丢东西。

★**改了配置之后 `pytest` 结果不会变。** 测试一律跑内置默认名册，
不读你的 `config.json`。一份会随运行者配置变结果的用例，不是用例。

---

## 日常五条命令

```bash
# 建一张派单（先建单，再把单号写进任务书——反过来一定会写错号）
ticket.py new --slot <岗位> --title "<一句话写清交付物>" \
  --source "<真源指针：某个文件:某一节>" --consumer "<实机消费者>" \
  --deliverable "<必须产出的文件路径>" --tier 乙 --internal --by <谁建的>

# 干活的人开工第一条
ticket.py receipt T-000123      # 返工态会把「上一轮为什么被退」一并带进新窗

# 交板
ticket.py submit T-000123 --evidence "<做到什么程度>" \
  --verify-command "<跑哪条命令能验>" --raw-output "<原样输出>"

# 判卷
ticket.py judge T-000123 --pass --by <谁判的> \
  --verdict "<怎么打开它：点哪里 / 跑哪条命令 / 看见什么>"

# 每天扫一眼
ticket.py digest                # 新单/停滞/阻塞/待复验/可并
```

每条都有 `--help`。网页台面：`ticket.py serve --open`。

完整功能、每一道闸的理由，看 `ticket-desk/README.md`。

---

## 什么时候别装

一条都不中的话，装了只是多一层要维护的东西：

- 你已经记不住现在有哪几件事在办
- 出现过「我以为它做完了，其实没有」
- 两扇窗改过同一个文件，或者抬过同一个递增号

**你已经有台账了（哪怕只是一个 txt 清单），也先别换。**
换台账的代价不在装，在「新旧两套并存两周，然后你放弃其中一套」。

---

## 以后想要完整版

随时可以回 [`1-导出记忆.md`](1-导出记忆.md) 走完整的两步。
**这条路不挡那条路**——工单台的数据独立存放，不会因为你后来做了接手而失效。
