# 第二步 · 在新窗口里导入与接手

**开一扇全新的窗**，读这一份 + 你刚导出的 `记忆导出.md`。

> ★**新窗口不要接你的老项目目录。** 它手上只该有那份导出件。
> 这样「不小心动了老项目」在结构上就不可能发生，不靠 AI 自觉。
> 缺什么就回老窗口补导一次——比让它去翻你的项目安全得多。

---

## 贴这一句

```
读 2-导入与接手.md，再读 记忆导出.md。
按它说的先做兼容检查、再给建议，这一轮不要改任何文件。
```

---

## 第一步 · 兼容检查（只读，不动手）

拿导出件和这套方法逐项对，然后**停下来报告**。要查这五件：

1. **老规矩里有没有和这套直接打架的。**
   逐条摆出来，★**不许自己挑一边**。
2. **这套的三条底线，老项目缺哪条**（缺才提，不缺别提）：
   - 至少两扇窗，沟通与执行分开
   - 「做完」是一句能验的话——点哪里 / 跑哪条命令 / 看见什么
   - 规矩改文件，不在对话里临时豁免
3. **术语撞车**——两边都有、但意思不一样的词。这个最坑，见下。
4. **老项目自己的优先级**——导出件第六节写了「以 X 为准」的，照它的。
   ★AI 觉得那个顺序不合理可以说，但**不许自己改过来**。
5. **导出件里明说没带的东西**（第七节）——哪些会影响接手，点出来让人决定要不要补导。

### ★ 术语撞车：两边都有、意思不一样

真实撞过的例子：某项目的「**工位**」指的是一个云端 AI，用完就关；
而这套里的「**位**」是长期存在的组织单元。同一个字，一个临时、一个常驻。
混起来，你会以为它在说另一件事。

**处置**：当场说出来，然后**一律用你的叫法**。
你叫「总工程师 / 研发工位 / 复核」，这套叫「策划窗 / 工位窗 / 验收」——
说的是同一件事，AI 要跟着你的词走，不许中途换。

---

## 第二步 · 报告（一屏之内）

```
导出件：读到 N 条硬规矩、N 条坑；第七节说明没带 <什么>
打架的：<逐条列，各自原文；没有就写"无">
撞车词：<两边同名不同义的词；没有就写"无">
缺的：  三条底线里缺 <哪几条>（都不缺就明说"都不缺"）
开窗：  <建议开几扇、怎么分，一句话理由>
```

开窗建议只看可数的三样：**模块几块、第一版几块、现在几扇窗在干活**。
★**不看工期**——一个做三个月的项目，第一天照样只有两扇窗。

| 现在几扇窗 | 建议 |
|---|---|
| **1 扇** | 开到 2 扇：一扇只谈、一扇只干。二者争同一份上下文，而**先耗尽的是谈的那一侧**——耗尽之后它开始丢弃最早那段，也就是你最初提的需求，且不会有任何提示 |
| **2～6 扇** | 窗够了。缺的通常是「谁在做什么」记得住 |
| **7 扇以上** | 你多半已经在跟"管事的"说话，而不是跟"干活的"说话。缺的是**并线次序**：两扇窗别改同一个文件、别抬同一个递增号 |

报告完**停住**。

---

## 第三步 · 两个征询 —— ★都要你点头

**你不点头，什么都不动。**
而且这两句只是**同意开始**，不是一次批准到底——真动手时还要一步一停，见第四步。

### 征询一：装不装工单台？

值得装的信号，**中两条才值得**：

- 你已经记不住现在有哪几件事在办
- 出现过「我以为它做完了，其实没有」
- 两扇窗改过同一个文件，或者抬过同一个递增号

**不装也能跑。** 工单台是省事的工具，不是这套方法的前提。

> 导出件第三节里如果已经有台账（工单、清单、看板），那就**先别换**。
> 换台账的代价不在装，在「新旧两套并存两周，然后你放弃其中一套」。

### 征询二：要不要合并成这套工作架构？

★**三个答案都是对的，默认是 B。**

| | 意思 | 什么时候选 |
|---|---|---|
| **A · 全合并** | 按这套的两扇窗 / 工位 / 验收重排工作模式 | 你原来那套本来就松散、或者你正想推倒重来 |
| **B · 只补缺的**（默认） | 保持原来的模式，只把第一步查出来**你缺的那几条**补进去 | 绝大多数情况 |
| **C · 什么都不改** | 报告收下，工作模式一个字不动 | **你那套已经比这套全**——见下 |

> **关于 C**：老项目如果已经有工单 / 判卷 / 复核 / 章程这类东西，
> 那它比这套完整，**正确答案就是 C**。
> AI 这时候该说「你的体系已经覆盖了这三条，我没有可补的」，
> **而不是**硬凑几条建议来显得这趟没白来。

---

## 第四步 · 动手（★一步一确认）

动手前先列清单：

> **接下来共几步、每一步动哪个文件、是新建还是修改。** 列完等你说"开始"。

然后一步一停：

```
第 N 步 / 共 M 步：<要做什么>
  动的文件：<路径>（新建 / 修改）
  → 要我做吗？
```

做完一步报一句，**停住等下一个"继续"**。
不许把"继续"理解成"剩下的都继续"。

★**凡是要改你老项目文件的**，多一道闸：先把**改前那几行原文**和**改后那几行**
摆出来，你点头才落盘。不许只说"我把 X 补进去了"。

★**改法：补进你自己原来那份文件里，不要新建第二份。**
同一个事实存两处，过几天必漂，而且不报错。

---

## 明确不做的事

- ❌ **不碰你的老项目目录**——新窗口手上只有导出件
- ❌ 不建 `宪法.md` / `项目.md` / `DECISIONS.md`，也不覆盖任何同名文件
- ❌ 不重排、不精简、不"优化"你的老记忆
- ❌ 不删你已有的规矩（哪怕看起来多余——它多半防着一次你没说的事故）
- ❌ 不动老记忆内部已有的优先级顺序
- ❌ 不装工单台，除非你明说要装
- ❌ 不改工作模式，除非你在征询二选了 A 或 B
- ❌ **不一次改完**——一步一停，改前先摆原文对照

> 说到底就一条：**这是你的项目，不是它的。**
> 它的默认动作是「问」，不是「做」。

---

## 冲突了谁赢

**老的赢。** 你那套是你项目的真源，这两份只是接手流程。

★**冲突不许 AI 自己挑**——逐条摆出来问你，一次问完，别一条一条来回。

---

## 什么时候该去用 md-first 那四份

接手之后**重新开了一条线**（另起一个新模块、新仓），那条线是从零开始的，
那时候去 <https://github.com/comkocs/md-first> 按规模挑一份，用在**那条线**上。

已经在跑的这条，继续用你原来那套。
