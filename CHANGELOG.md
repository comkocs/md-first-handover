# 更新记录

版本号与 [md-first](https://github.com/comkocs/md-first) **保持一致**：两仓配套使用，
同一个号指的是同一套口径。`1.0` 是第一版，之后的小更新往后排 `1.01`、`1.02`。

> 本仓从 **1.03** 起开始记版本。在那之前它跟着 md-first 一起改，但没有单独编号——
> 补造几个历史版本号只会让人以为那时有过对应的发布。

---

## 1.03

全部来自一份**接手实践反馈**：有人拿这两份文档去接一个已经在跑的项目，
两天里撞到的坑连同修法一起寄了回来。改的都是**文档的前提写错了**的地方——
不是漏写，是写了一个实际不成立的假设。

### 改（前提不成立）

- **「新窗口不要接你的老项目目录」这条靠不住，也不该靠。**
  原文写着「这样『不小心动了老项目』在结构上就不可能发生」。
  可实际上没人会为了一份流程另开一个空目录——桌面版就是在项目目录里开窗的。
  于是那个「结构上不可能」的前提当场失效，而文档还在按它写。
  改成：**开在哪都行，但接手轮一律零写入**，并给一条自检命令
  （收尾时 `git status --short` 必须是空的）。
  ★文档不该假装一件会发生的事不会发生——那样它连一句防备都不会写。

### 加（两条最贵的）

- **会话文件夹放在 git 仓外面。**
  桌面版一旦发现会话文件夹在 git 仓里，就会挂上分支标签并给一个分支切换器，
  而那切的是**仓根的真实 HEAD**。真实后果：仓根被反复切到各种功能分支、
  有人的登记提交落在功能分支上、还有一扇窗正在仓根跑九分钟的回归，
  跑到一半仓根被切走了而它自己不知道。
  顺带解决另一件事：桌面版的自动记忆**按目录归档**，几个位的会话挤在同一个目录里，
  记忆互相可见——新窗会读到别的位的记忆然后把别人的活也接了。一位一个目录，记忆自然分家。
- **激活句必须带位名与章程路径。**
  只说「查询工单」开出来的窗没有身份锚点。真撞过：一扇新窗读完记忆
  把别的位的活一并接了——判了别人的单、解了别人的阻塞，**留痕全署着别位的名**，
  事后查账两条时间线对不上。

### 加（问清楚，别自己猜）

- **术语撞车把「工单」「位/工位」列为逐条必问**，不许并成一句「术语没冲突」。
  真撞过：一边的「工单」是产品里的一个页面、另一边是任务台账；
  一边的「工位」是临时窗、另一边是常驻位。
  **同一句话两个人理解成两件事，而且都不会发现。**
- **报告模板加一行必问项：登记类改动（章程/裁定/台账）能不能直接进主线。**
  很多项目有「主线不直接提交」的规矩，而本流程的默认动作是「补进你原来那份文件里」，
  两者正面打架。不问的结果是接手窗自己挑一边，而它挑哪边你都不知道。
- **明确不做的事里加一条：接手之前的老活不补成工单。**
  它们没有单号、没有当时的判据，补出来的是**看着像真的假记录**。
